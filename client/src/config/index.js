import { mainRoutes, adminRoutes, subRoute, doctorRoutes, patientRoutes } from './routes';

const configs = {
    mainRoutes,
    adminRoutes,
    doctorRoutes,
    patientRoutes,
    subRoute,
};

export default configs;
